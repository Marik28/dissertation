```mermaid
sequenceDiagram
    participant User
    participant QComboBox
    participant Device
    participant Relay
    participant SensorWorker
    participant SensorManager
    participant OwenClient
    participant Solver
    participant SensorCharacteristicsService
    participant TemperatureCalculationThread
    participant TemperaturePlotManager
    participant QTableWidget
    participant Database
```